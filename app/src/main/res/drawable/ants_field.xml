<?xml version="1.0" encoding="utf-8"?>
<!-- 입력칸. 어두운 배경에서 밑줄 하나만 있으면 어디를 눌러야 할지 모른다 —
     면으로 채우고 테두리를 준다. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/ants_surface_alt" />
    <corners android:radius="16dp" />
    <stroke android:width="1dp" android:color="@color/ants_divider" />
</shape>
